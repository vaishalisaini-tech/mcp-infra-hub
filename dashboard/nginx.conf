server {
    listen 80;
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;   # SPA routing fallback
    }
    location /api/ {
      proxy_pass http://audit-api:8080/api/;
    }
}

